<%= form_for(current_user.favorites.find_by(microposts_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfavorite", class: "btn" %>
<% end %>