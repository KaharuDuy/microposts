<%= form_for(current_user.favorites.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :microposts_id, @user.id %></div>
  <%= f.submit "Favorite", class: "btn btn-warning btn-xs" %>
<% end %>